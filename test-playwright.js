const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('BROWSER LOG:', msg.text())); page.on('pageerror', err => console.log('BROWSER ERROR:', err)); await page.goto('http://localhost:3000/canticosadmin.html'); await page.waitForTimeout(2000); await page.fill('#search-input', 'Dios'); await page.waitForTimeout(2000); const buttons = await page.$$('.himnario-tab'); if (buttons.length > 2) { await buttons[2].click(); } await page.waitForTimeout(2000); await browser.close(); })();
